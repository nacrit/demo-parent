<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mybatis.dao.TempMapper">
    <update id="tempUpdate">
        update demo_mybatis_temp
        set `name` = #{name}
        where id = #{id}
    </update>
</mapper>